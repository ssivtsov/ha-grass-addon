configuration:
  user_email:
    name: Grass account email
    description: >-
      The email of your getgrass.io account. Used for auto-login. Leave
      empty to log in entirely by hand through the noVNC web UI.
  user_password:
    name: Grass account password
    description: The password of your getgrass.io account (used for auto-login).
  try_autologin:
    name: Try auto-login
    description: >-
      Attempt an automatic login with the email/password above, following
      Grass's email -> "Use Password Instead" -> password flow. If it
      misfires, log in manually through the noVNC web UI; either way the
      session is saved to /data and persists.
  email_xy:
    name: Email field position
    description: >-
      Advanced. "x,y" click position (offset from the Grass window's top-left)
      of the email field during auto-login. Adjust using the debug screenshots
      if the click misses. Default "175,225".
  continue_xy:
    name: Continue button position
    description: >-
      Advanced. "x,y" click position of the CONTINUE button. Default "175,296".
  use_password_xy:
    name: "'Use Password Instead' position"
    description: >-
      Advanced. "x,y" click position of the "Use Password Instead" link on the
      code screen. Default "175,440".
  password_xy:
    name: Password field position
    description: >-
      Advanced. "x,y" click position of the password field. Default "175,225".
  signin_xy:
    name: Sign-in button position
    description: >-
      Advanced. "x,y" click position of the SIGN IN button. Default "175,296".
  debug_screenshots:
    name: Debug screenshots
    description: >-
      Save a screenshot of each auto-login step to /share/grass-debug/
      (grass-step*.png), browsable with the File editor / Samba add-ons, so
      you can see exactly what the app showed when credentials were typed.
      Turn off once debugging is done.
  vnc_password:
    name: VNC password
    description: "Password to access the noVNC / VNC interface. Default: money4band."
  vnc_resolution:
    name: VNC resolution
    description: Virtual screen resolution, for example 1280x720.
  allow_debug:
    name: Allow debug (deprecated)
    description: No longer used; safe to remove.
  headless:
    name: Headless mode (deprecated)
    description: No longer used; safe to remove.
  max_retry_multiplier:
    name: Max retry multiplier (deprecated)
    description: No longer used; safe to remove.
network:
  5900/tcp: VNC server port.
  6080/tcp: noVNC web interface port.
